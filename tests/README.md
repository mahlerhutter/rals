# Conformance Tests

This directory contains the test suite for RALS validation.

The `validate-examples.yml` GitHub workflow runs automatically against the `examples/` folder. 
In the future, automated tests for parser implementations will be added here to guarantee consistent parsing of the confidentiality tiers.
